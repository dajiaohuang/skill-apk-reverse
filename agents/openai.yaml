interface:
  display_name: "APK Reverse"
  short_description: "Install and use APK unpacking tools"
  default_prompt: "Use $apk-reverse to inspect an APK, install missing unpacking tools, validate them, and run a first static analysis."
